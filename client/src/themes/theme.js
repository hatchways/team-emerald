import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  typography: {
    fontFamily: '"Roboto"',
  },
  primary: '#f04040',
  secondary: '#1f1f1f',
  error: '#d8000c',
  bgcolor: '#f6f6f6',
});

export default theme;
